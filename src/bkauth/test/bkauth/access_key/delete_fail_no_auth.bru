meta {
  name: delete_fail_no_auth
  type: http
  seq: 9
}

delete {
  url: http://{{host}}:{{port}}/api/v1/apps/demo/access-keys/{{access_key_id}}
  body: none
  auth: none
}

assert {
  res.status: eq 401
  res.body.code: eq 1903401
  res.body.message: eq unauthorized:app code and app secret required
}