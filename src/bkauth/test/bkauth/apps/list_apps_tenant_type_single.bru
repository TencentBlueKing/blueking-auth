meta {
  name: list_apps_tenant_mode_single
  type: http
  seq: 13
}

get {
  url: http://{{host}}:{{port}}/api/v1/apps?tenant_mode=single&tenant_id=default
  body: json
  auth: none
}

params:query {
  tenant_mode: single
  tenant_id: default
}

headers {
  X-Bk-App-Code: {{x_bk_app_code}}
  X-Bk-App-Secret: {{x_bk_app_secret}}
}

assert {
  res.status: eq 200
  res.body.message: eq ok
  res.body.data.count: eq 1
}
